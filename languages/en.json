{
  "blueprint.name": "Smart Home Battery",
  "blueprint.description": "Generic Home Assistant blueprint for up to four batteries. It allocates discharge from house demand, can trigger opportunistic charging during real export, and writes a signed power target for each battery into a numeric entity, with optional custom actions for integrations that need a separate mode switch.",
  "blueprint.version.line": "Version: {version}",
  "message.invalid_config_prefix": "Invalid battery blueprint configuration:",
  "message.invalid_sensor": "House power sensor {{ house_power_sensor }} is unavailable or not numeric.",
  "trace.validation_gate": "Validation and sensor guardrails",
  "trace.invalid_config_branch": "Invalid battery configuration",
  "trace.stop_invalid_config": "Stop on configuration error",
  "trace.invalid_sensor_branch": "Invalid house power sensor",
  "trace.stop_invalid_sensor": "Stop on sensor error",
  "trace.write_target_suffix": "update signed target",
  "trace.target_update_branch_suffix": "target update required",
  "trace.prepare_target_context_suffix": "prepare target update context",
  "trace.select_target_service_suffix": "select target entity service",
  "trace.write_number_target_suffix": "write target to number entity",
  "trace.write_input_number_target_suffix": "write target to input_number entity",
  "trace.run_discharge_actions_suffix": "run discharge actions",
  "trace.discharge_actions_branch_suffix": "discharge actions required",
  "trace.prepare_discharge_context_suffix": "prepare discharge action context",
  "trace.execute_discharge_actions_suffix": "execute discharge action sequence",
  "trace.run_charge_actions_suffix": "run charge actions",
  "trace.charge_actions_branch_suffix": "charge actions required",
  "trace.prepare_charge_context_suffix": "prepare charge action context",
  "trace.execute_charge_actions_suffix": "execute charge action sequence",
  "validation.no_target_entity.suffix": "is enabled by its state of charge sensor, but no Target power entity is configured. Add a `number` or `input_number` entity, or clear the state of charge sensor to disable this battery slot.",
  "validation.no_power.suffix": "has a target power entity configured, but both maximum power limits are set to 0 W. Set a non-zero Maximum discharge power or Maximum charge power to make this slot usable.",
  "slot.1": "Battery 1",
  "slot.2": "Battery 2",
  "slot.3": "Battery 3",
  "slot.4": "Battery 4",
  "input.house_power_sensor.name": "House power sensor",
  "input.house_power_sensor.description": "Main house power sensor. This selector only shows power sensors. Use a signed sensor in watts with positive values for import and negative values for export.",
  "input.house_power_is_smoothed.name": "House power sensor already smoothed",
  "input.house_power_is_smoothed.description": "Enable this when the selected house power sensor already exposes a moving average or another filtered value that should be trusted as-is. When enabled, the blueprint uses that sensor value directly and does not rebuild the underlying house demand by adding back active managed battery targets.",
  "input.blocking_entities.name": "Blocking entities",
  "input.blocking_entities.description": "Optional entities that must all stay off for the blueprint to run. If any selected entity is on, unknown, or unavailable, the blueprint can force every managed battery back to neutral only when that blocking entity changed state, then it stops all custom charge and discharge actions and ignores further blocked runs until every blocker is back to off.",
  "input.battery.soc_sensor.name": "State of charge sensor",
  "input.battery.soc_sensor.description": "Leave empty to ignore this battery slot. Otherwise, this selector only shows battery sensors that report a charge percentage, and the chosen sensor enables the slot. An enabled slot must also expose a numeric target entity and at least one non-zero power limit.",
  "input.battery.max_discharge_w.name": "Maximum discharge power",
  "input.battery.max_discharge_w.description": "Manual discharge limit used by the allocator for this battery.",
  "input.battery.max_charge_w.name": "Maximum charge power",
  "input.battery.max_charge_w.description": "Manual charge limit used by the allocator for opportunistic charging. Set to 0 if this battery must never be charged by the blueprint.",
  "input.battery.priority_discharge.name": "Priority on discharge",
  "input.battery.priority_discharge.description": "Prioritized batteries are discharged first, ordered by their state of charge. Non-priority batteries come afterwards, also ordered by state of charge.",
  "input.battery.cooldown_seconds.name": "Command cooldown",
  "input.battery.cooldown_seconds.description": "Minimum delay between two active target updates for this battery. Set to 0 to disable it. Writing `0` or flipping the sign still happens immediately so the blueprint can stop or reverse a battery without waiting. While a battery is cooling down, the blueprint keeps its current active target and redistributes only the remaining house demand or export surplus to the other batteries.",
  "input.battery.target_power_entity.name": "Target power entity",
  "input.battery.target_power_entity.description": "The `number` or `input_number` entity written by the blueprint. The target is signed in watts: positive for discharge, negative for charge, `0` for stop. If this battery should charge, the selected entity must accept negative values.",
  "input.battery.discharge_actions.name": "Discharge actions",
  "input.battery.discharge_actions.description": "Optional custom actions executed when this battery should actively discharge. They run on every active discharge target update. Variables such as battery_slot, battery_soc, target_power_w, target_discharge_w, house_power_w, import_need_w and export_surplus_w are available inside these actions.",
  "input.battery.charge_actions.name": "Charge actions",
  "input.battery.charge_actions.description": "Optional custom actions executed when this battery should actively charge from surplus export. They run on every active charge target update. Variables such as battery_slot, battery_soc, target_power_w, target_charge_w and export_surplus_w are available inside these actions."
}
