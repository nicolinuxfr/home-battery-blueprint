{
  "blueprint.name": "Smart Home Battery",
  "blueprint.description": "Generic Home Assistant blueprint for up to four batteries. It allocates discharge from house demand, can trigger opportunistic charging during real export, and drives each battery only through custom actions.",
  "blueprint.version.line": "Version: {version}",
  "message.invalid_config_prefix": "Invalid battery blueprint configuration:",
  "message.invalid_sensor": "House power sensor {{ house_power_sensor }} is unavailable or not numeric.",
  "validation.no_interface.suffix": "is configured but has no usable charge or discharge actions.",
  "slot.1": "Battery 1",
  "slot.2": "Battery 2",
  "slot.3": "Battery 3",
  "slot.4": "Battery 4",
  "input.house_power_sensor.name": "House power sensor",
  "input.house_power_sensor.description": "Main house power sensor. The blueprint expects positive values for import and negative values for export.",
  "input.blocking_entities.name": "Blocking entities",
  "input.blocking_entities.description": "Optional entities that must all stay off for the blueprint to run. If any selected entity is on, unknown, or unavailable, the blueprint forces every managed battery back to neutral and stops all custom charge and discharge actions.",
  "input.battery.soc_sensor.name": "State of charge sensor",
  "input.battery.soc_sensor.description": "Leave empty to ignore this battery slot. Otherwise, this sensor enables the slot and provides its current charge percentage.",
  "input.battery.actual_power_sensor.name": "Actual power sensor",
  "input.battery.actual_power_sensor.description": "Optional signed power sensor. Positive values are assumed to mean discharge and negative values charge. The blueprint uses it as a safety hint and to help stop actions.",
  "input.battery.max_discharge_w.name": "Maximum discharge power",
  "input.battery.max_discharge_w.description": "Manual discharge limit used by the allocator for this battery.",
  "input.battery.max_charge_w.name": "Maximum charge power",
  "input.battery.max_charge_w.description": "Manual charge limit used by the allocator for opportunistic charging. Set to 0 if this battery must never be charged by the blueprint.",
  "input.battery.priority_discharge.name": "Priority on discharge",
  "input.battery.priority_discharge.description": "Prioritized batteries are discharged first, ordered by their state of charge. Non-priority batteries come afterwards, also ordered by state of charge.",
  "input.battery.cooldown_seconds.name": "Command cooldown",
  "input.battery.cooldown_seconds.description": "Minimum delay between two active commands for this battery. Stop and neutralization actions can still run immediately for safety.",
  "input.battery.set_discharge_actions.name": "Set discharge actions",
  "input.battery.set_discharge_actions.description": "Optional custom actions executed when this battery should actively discharge. Variables such as battery_slot, battery_soc, target_discharge_w, house_power_w, import_need_w and export_surplus_w are available inside these actions. For integrations like Zendure, this can write a signed helper entity that the vendor integration consumes.",
  "input.battery.stop_discharge_actions.name": "Stop discharge actions",
  "input.battery.stop_discharge_actions.description": "Optional custom actions executed when discharge must stop. Configure them as idempotent if possible, especially for batteries controlled only through actions.",
  "input.battery.set_charge_actions.name": "Set charge actions",
  "input.battery.set_charge_actions.description": "Optional custom actions executed when this battery should actively charge from surplus export. Variables such as battery_slot, battery_soc, target_charge_w and export_surplus_w are available inside these actions. For integrations like Zendure, this can write a negative signed helper value.",
  "input.battery.stop_charge_actions.name": "Stop charge actions",
  "input.battery.stop_charge_actions.description": "Optional custom actions executed when charging must stop. Configure them as idempotent if possible, especially for batteries controlled only through actions."
}
